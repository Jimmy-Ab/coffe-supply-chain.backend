const requestCertificate = require("./requestCertificate");
const grantCertificate = require("./grantCertificate");
const revokeCertificate = require("./revokeCertificate");
const deleteCertificate = require("./deleteCertificate");
const getAllCertificate = require("./getAllCertificate");
const getCertificate = require("./getCertificate");
module.exports = {
  requestCertificate,
  grantCertificate,
  revokeCertificate,
  deleteCertificate,
  getAllCertificate,
  getCertificate,
};
